-- ##################
-- # File managed by puppet, don't edit
-- ##################
grant usage tables on *.* to '<%= scope['sonar::backup_user'] %>'@'%' identified by '<%= scope['sonar::backup_password'] %>';
GRANT SELECT, LOCK TABLES ON sonar.* to '<%= scope['sonar::backup_user'] %>'@'%';
